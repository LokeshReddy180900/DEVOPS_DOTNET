pipeline {
    agent any

    environment {
        dotnet = "/home/ubuntu/.dotnet"
        sln = '/home/ubuntu/DEVOPS_DOTNET/MyWebApp'
    }

    stages {
        stage("Building"){
            steps {
                echo "Building SourceCode"
                sh '${dotnet} dotnet run ${sln} '
                echo "Built Successfully"
            }
        }
    }
}