pipeline {
    agent any

    stages {
        stage("Building"){
            steps {
                echo "Building SourceCode"
                sh 'dotnet build'
                echo "Built Successfully"
            }
        }
    }
}